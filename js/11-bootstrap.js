document.addEventListener('DOMContentLoaded',async()=>{updateLanguage();initDragAndDrop();isSupabaseReady=true;initPi();await restoreSession();refreshPiPrice();loadAllProducts();initLocations()});setInterval(refreshPiPrice,60000);
